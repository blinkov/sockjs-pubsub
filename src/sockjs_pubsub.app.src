{application, sockjs_pubsub, [
	{description, "Publish/Subscribe for SockJS"},
	{vsn, "0.1"},
	{modules, []},
	{registered, [sockjs_pubsub_sup]},
	{applications, [
		kernel,
		stdlib
	]},
	{mod, {sockjs_pubsub, []}},
	{env, []}
]}.
